<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- This allows the camera provider access to the app's secure isolated cache directory -->
    <cache-path name="captured_images" path="." />
</paths>